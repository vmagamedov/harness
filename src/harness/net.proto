syntax = "proto3";

package harness.net;

message Socket {
    string host = 1;
    uint32 port = 2;
}

message Pipe {
    string name = 1;
    uint32 mode = 2;
}

message Address {
    oneof type {
        Socket socket = 1;
        string pipe = 2;
    }
}
