syntax = "proto3";

package harness.grpc;

import "harness/wire.proto";
import "harness/net.proto";

message Channel {
    harness.net.Socket address = 1 [(harness.wire).protocol = GRPC];
}

message Server {
    harness.net.Socket bind = 1 [(harness.wire).protocol = GRPC];
}
